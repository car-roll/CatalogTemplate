pipeline {
    agent {label "master"}
    stages {
        stage('Build') { 
            steps {
              script{
                if(testVar == false){
                  properties([pipelineTriggers([cron('* * * * *')])])
                }
                else{
                  properties([pipelineTriggers([cron('')])])
                }
              }
            }
        }
    }
}
